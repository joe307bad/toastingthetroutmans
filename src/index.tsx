import * as React from 'react';
import * as ReactDOM from 'react-dom';

import { Main } from './components/Main';

/**
 * ReactDOM.Render responsible for rendering the entire applications
 */
ReactDOM.render(
    <Main />,
    document.getElementById('root')
);
